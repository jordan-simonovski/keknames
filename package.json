{
  "name": "keknames",
  "version": "1.0.0",
  "description": "Online multiplayer Codenames clone with El Risitas-themed card art",
  "main": "dist/index.js",
  "scripts": {
    "build:server": "tsc",
    "start": "node dist/index.js",
    "dev:server": "tsc --watch & node --watch dist/index.js",
    "dev:client": "npm run dev --prefix client",
    "dev": "npm run build:server && (npm run dev:server & npm run dev:client)",
    "build": "tsc && npm run build --prefix client",
    "lint": "npm run lint --prefix client",
    "test": "npm run test --prefix client",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.21.0",
    "helmet": "^8.1.0",
    "socket.io": "^4.7.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.3.0",
    "typescript": "^5.9.3"
  }
}
